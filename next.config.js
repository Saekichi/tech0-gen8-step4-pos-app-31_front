/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig

// next.config.js
module.exports = {
  reactStrictMode: true,
  // swcMinify: true, // これを削除またはコメントアウト
};
